#include "main.h"
#include <stdlib.h>
#include <string.h>

/**
 * string_nconcat - creates an array of characters
 * @s1: char
 * @s2: char
 * @n: unsigned int
 * Return: none
*/

char *string_nconcat(char *s1, char *s2, unsigned int n)
{
	void *t;

	t = malloc(b);
	if (t == NULL)
		exit(98);
	return (t);
}
