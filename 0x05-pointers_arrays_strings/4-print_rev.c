#include "_putchar.h"
#include <stdio.h>
#include <string.h>

/**
* _puts - prints out the string
* @str: characters
* Return: None
*/

void _puts(char *str)
{
	int len;
	char str1;
	
	len = strlen(str);
	str1 = str + len - 1;
	while (str1 >= st)
	{
		puts(str1);
		str1--;r
}
